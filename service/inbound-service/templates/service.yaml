apiVersion: v1
kind: Service
metadata:
  name: {{ include "inbound-service.fullname" . }}
  labels:
    {{- include "inbound-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inbound-service.selectorLabels" . | nindent 4 }}
