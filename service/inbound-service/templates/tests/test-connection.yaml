apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inbound-service.fullname" . }}-test-connection"
  labels:
    {{- include "inbound-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inbound-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
