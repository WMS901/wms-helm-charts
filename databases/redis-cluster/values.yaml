replicaCount: 6

image:
  repository: redis
  tag: "7.2"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  clusterIP: None
  port: 6379

persistence:
  enabled: true
  size: 1Gi
  storageClass: redis-cluster

config:
  appendonly: "yes"
  save: "60 1"

pvcPrefix: redis

